# Copie para .streamlit/secrets.toml (local) OU cole em
# Streamlit Cloud -> App Settings -> Secrets.
# NUNCA commitar o arquivo secrets.toml real (ele esta no .gitignore).

[database]
uri = "postgresql+psycopg2://data_iesb:iesb@bigdata.dataiesb.com:5432/iesb"
